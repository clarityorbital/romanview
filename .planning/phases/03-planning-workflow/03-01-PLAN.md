---
phase: 03-planning-workflow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/rollRange.ts
  - src/lib/urlState.ts
  - src/lib/ds9Export.ts
  - src/lib/__tests__/rollRange.test.ts
  - src/lib/__tests__/urlState.test.ts
  - src/lib/__tests__/ds9Export.test.ts
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03]

must_haves:
  truths:
    - "Roll range computation returns nominal V3PA +/-15 degrees for an observable target"
    - "Roll range returns null for targets outside the 54-126 degree Sun separation window"
    - "URL encode/decode round-trips all observation parameters (ra, dec, name, pa, date) without loss"
    - "URL encoding handles target names with spaces and special characters correctly"
    - "DS9 export generates exactly 18 polygon regions with FK5 coordinates and DS9 v4.1 header"
    - "Each DS9 polygon has exactly 4 coordinate pairs matching the SCA corner count"
  artifacts:
    - path: "src/lib/rollRange.ts"
      provides: "Roll range computation for PA slider"
      exports: ["computeRollRange"]
    - path: "src/lib/urlState.ts"
      provides: "URL hash encode/decode for shareable state"
      exports: ["encodeToHash", "decodeFromHash", "ObservationParams"]
    - path: "src/lib/ds9Export.ts"
      provides: "DS9 region file text generation"
      exports: ["generateDS9Regions"]
    - path: "src/lib/__tests__/rollRange.test.ts"
      provides: "Unit tests for roll range"
      min_lines: 30
    - path: "src/lib/__tests__/urlState.test.ts"
      provides: "Unit tests for URL state"
      min_lines: 30
    - path: "src/lib/__tests__/ds9Export.test.ts"
      provides: "Unit tests for DS9 export"
      min_lines: 40
  key_links:
    - from: "src/lib/rollRange.ts"
      to: "src/lib/coordinates.ts"
      via: "imports positionAngle and angularSeparation"
      pattern: "import.*positionAngle.*from.*coordinates"
    - from: "src/lib/ds9Export.ts"
      to: "src/lib/wcs.ts"
      via: "imports v2v3ToSky for corner projection"
      pattern: "import.*v2v3ToSky.*from.*wcs"
    - from: "src/lib/ds9Export.ts"
      to: "src/lib/roman.ts"
      via: "imports WFI_DETECTORS and WFI_BORESIGHT"
      pattern: "import.*WFI_DETECTORS.*from.*roman"
---

<objective>
Create the three core library modules for Phase 3 (roll range, URL state, DS9 export) using TDD. These are pure functions with well-defined inputs and outputs -- ideal TDD candidates.

Purpose: Establish the computation and data layers that the UI components (Plan 02) will consume. All three modules are pure functions with no React dependencies, making them independently testable.

Output: Three library modules with comprehensive test suites, all tests green.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-planning-workflow/03-RESEARCH.md

@src/lib/coordinates.ts
@src/lib/wcs.ts
@src/lib/roman.ts
@src/lib/constraints.ts
@src/lib/siaf.ts

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase. -->

From src/lib/coordinates.ts:
```typescript
export function positionAngle(targetRaDeg: number, targetDecDeg: number, sunRaDeg: number, sunDecDeg: number): number;
export function angularSeparation(ra1Deg: number, dec1Deg: number, ra2Deg: number, dec2Deg: number): number;
```

From src/lib/wcs.ts:
```typescript
export function v2v3ToSky(v2: number, v3: number, boresightV2: number, boresightV3: number, targetRa: number, targetDec: number, v3paDeg: number): { ra: number; dec: number };
```

From src/lib/roman.ts:
```typescript
export interface DetectorInfo {
  id: string;
  v2Ref: number;
  v3Ref: number;
  v3IdlYAngle: number;
  corners_v2v3: [number, number][];
}
export const WFI_DETECTORS: DetectorInfo[];
export const WFI_BORESIGHT: { v2: number; v3: number };
```

From src/lib/constraints.ts:
```typescript
export const SUN_EXCLUSION_ANGLE = 54;
export const ANTI_SUN_EXCLUSION_ANGLE = 36;
export function getSunPosition(date: Date): { ra: number; dec: number };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roll range and URL state modules with tests</name>
  <files>src/lib/rollRange.ts, src/lib/urlState.ts, src/lib/__tests__/rollRange.test.ts, src/lib/__tests__/urlState.test.ts</files>
  <behavior>
    rollRange.test.ts:
    - computeRollRange returns { nominal, min, max } where min = nominal - 15 and max = nominal + 15 for a target at ~90 degrees Sun separation
    - computeRollRange returns null when Sun separation < 54 degrees (too close to Sun)
    - computeRollRange returns null when Sun separation > 126 degrees (anti-Sun exclusion, since 180-126=54 and anti-Sun sep < 36)
    - Nominal V3PA matches positionAngle() output for the same inputs
    - Edge case: target near ecliptic pole (always observable) returns valid range

    urlState.test.ts:
    - encodeToHash({ra: 10.6847, dec: 41.2687}) produces hash containing "ra=10.68470" and "dec=41.26870"
    - decodeFromHash round-trips all params: ra, dec, name, pa, date
    - Target names with spaces ("NGC 1234") survive encode/decode (URLSearchParams handles encoding)
    - Target names with special chars ("alpha Cen A") survive encode/decode
    - Missing params decode as undefined (not NaN or empty string)
    - encodeToHash omits undefined fields (no "ra=undefined" in output)
  </behavior>
  <action>
    RED: Write failing tests for both modules first.

    rollRange.ts -- Create `computeRollRange(targetRa, targetDec, sunRa, sunDec)` that:
    1. Computes Sun separation using angularSeparation from coordinates.ts
    2. Returns null if sunSep < 54 or sunSep > 126 (not observable)
    3. Computes nominal V3PA using positionAngle from coordinates.ts
    4. Returns { nominal, min: nominal - 15, max: nominal + 15 }

    urlState.ts -- Create `ObservationParams` interface, `encodeToHash()`, and `decodeFromHash()` that:
    1. ObservationParams: { ra?: number, dec?: number, name?: string, pa?: number, date?: string }
    2. encodeToHash: builds URLSearchParams, sets only defined fields, ra/dec to 5 decimal places, pa to 1 decimal place, returns '#' + params.toString()
    3. decodeFromHash: strips leading '#', parses URLSearchParams, returns ObservationParams with undefined for missing fields (use parseFloat, check isNaN)

    GREEN: Implement minimal code to pass all tests.
    REFACTOR: Clean up if needed.
  </action>
  <verify>
    <automated>cd /workspace/RomanView && npx vitest --run src/lib/__tests__/rollRange.test.ts src/lib/__tests__/urlState.test.ts</automated>
  </verify>
  <done>Both modules export their functions, all tests pass. computeRollRange correctly identifies observable/unobservable targets and returns +/-15 degree range. URL encode/decode handles all param types including names with spaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DS9 region file export module with tests</name>
  <files>src/lib/ds9Export.ts, src/lib/__tests__/ds9Export.test.ts</files>
  <behavior>
    ds9Export.test.ts:
    - generateDS9Regions returns string starting with "# Region file format: DS9 version 4.1"
    - Output contains "fk5" coordinate system line
    - Output contains exactly 18 polygon lines (one per SCA)
    - Each polygon line contains exactly 4 RA/Dec coordinate pairs (8 comma-separated numbers after "polygon(")
    - Each polygon line has text tag matching detector ID (e.g., "text={WFI01}")
    - All RA values are in range [0, 360) and all Dec values are in range [-90, 90]
    - downloadDS9Regions creates and triggers a Blob download (mock DOM test)
  </behavior>
  <action>
    RED: Write failing tests for DS9 export.

    ds9Export.ts -- Create `generateDS9Regions(targetRa, targetDec, v3pa)` that:
    1. Writes DS9 v4.1 header line
    2. Writes global properties line (color=cyan, font, etc.)
    3. Writes "fk5" coordinate system
    4. For each detector in WFI_DETECTORS:
       a. Project each corner from V2V3 to sky using v2v3ToSky(v2, v3, WFI_BORESIGHT.v2, WFI_BORESIGHT.v3, targetRa, targetDec, v3pa)
       b. Format as "polygon(ra1,dec1,ra2,dec2,...) # text={WFI01} color=cyan"
       c. RA/Dec formatted to 7 decimal places
    5. Return joined with newlines + trailing newline

    Also create `downloadDS9Regions(targetRa, targetDec, v3pa, targetName?)` that:
    1. Calls generateDS9Regions to get text content
    2. Creates Blob with type 'text/plain'
    3. Creates <a> element with download attribute, filename like "romanview_{targetName}_{pa}.reg"
    4. Triggers click and cleans up (revokeObjectURL)

    GREEN: Implement to pass all tests.
    REFACTOR: Clean up.
  </action>
  <verify>
    <automated>cd /workspace/RomanView && npx vitest --run src/lib/__tests__/ds9Export.test.ts</automated>
  </verify>
  <done>DS9 export generates valid .reg file text with 18 FK5 polygons. Download function creates and triggers Blob download. All tests green.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions and all new tests pass:
```bash
cd /workspace/RomanView && npx vitest --run --reporter=verbose
```
All existing tests (coordinates, wcs, siaf, parseCoords, simbad, vizier) still pass alongside the 3 new test files.
</verification>

<success_criteria>
- rollRange.ts exports computeRollRange with correct +/-15 deg range and observability check
- urlState.ts exports encodeToHash/decodeFromHash with round-trip fidelity
- ds9Export.ts exports generateDS9Regions producing valid DS9 v4.1 FK5 polygon text for all 18 SCAs
- All 3 new test files pass with comprehensive coverage of happy path and edge cases
- Full test suite green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-workflow/03-01-SUMMARY.md`
</output>
