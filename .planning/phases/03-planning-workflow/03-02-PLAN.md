---
phase: 03-planning-workflow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/App.tsx
  - src/components/ui/PASlider.tsx
  - src/components/ui/ShareButton.tsx
  - src/components/ui/ExportButton.tsx
  - src/components/ui/FocalPlaneView.tsx
  - src/components/scene/WFIFootprint.tsx
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03]

must_haves:
  truths:
    - "PA slider controls footprint rotation in both sky view and focal plane view simultaneously"
    - "PA slider range is constrained to nominal V3PA +/-15 degrees with visual indicator of the nominal value"
    - "PA slider shows both V3PA and derived APA values"
    - "Slider is disabled with message when target is not observable"
    - "URL hash updates when target, PA, or date changes -- and loading a URL with hash hydrates the correct state"
    - "Share button copies current URL to clipboard"
    - "Export button downloads a .reg file with the current footprint"
    - "Opening the app with no hash shows default behavior (no crash, no stale state)"
  artifacts:
    - path: "src/components/ui/PASlider.tsx"
      provides: "Interactive PA slider with roll range visualization"
      min_lines: 40
    - path: "src/components/ui/ShareButton.tsx"
      provides: "Copy-URL-to-clipboard button"
      min_lines: 15
    - path: "src/components/ui/ExportButton.tsx"
      provides: "DS9 region file download button"
      min_lines: 15
    - path: "src/App.tsx"
      provides: "Central PA state, URL sync, and prop threading"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/rollRange.ts"
      via: "computeRollRange for PA slider bounds"
      pattern: "import.*computeRollRange.*from.*rollRange"
    - from: "src/App.tsx"
      to: "src/lib/urlState.ts"
      via: "encodeToHash/decodeFromHash for URL sync"
      pattern: "import.*encodeToHash.*from.*urlState"
    - from: "src/components/ui/PASlider.tsx"
      to: "src/App.tsx"
      via: "receives nominalPA, minPA, maxPA, currentPA, onPAChange props"
      pattern: "onPAChange"
    - from: "src/components/ui/ExportButton.tsx"
      to: "src/lib/ds9Export.ts"
      via: "calls downloadDS9Regions on click"
      pattern: "import.*downloadDS9Regions.*from.*ds9Export"
    - from: "src/App.tsx"
      to: "src/components/scene/WFIFootprint.tsx"
      via: "passes effectiveV3PA instead of letting component compute its own"
      pattern: "v3pa.*effectiveV3PA"
    - from: "src/App.tsx"
      to: "src/components/ui/FocalPlaneView.tsx"
      via: "passes v3pa prop instead of sunPosition for PA computation"
      pattern: "v3pa.*effectiveV3PA"
---

<objective>
Wire the three library modules from Plan 01 into the React UI: lift PA state to App level, create PA slider / share / export UI components, wire URL state sync, and update WFIFootprint and FocalPlaneView to accept V3PA as a prop instead of computing it independently.

Purpose: This transforms the app from a static visualization into an interactive planning tool where users control the PA, share their setup, and export detector footprints.

Output: Working PA slider, share button, export button, and URL state sync -- all wired into the existing app layout.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-planning-workflow/03-RESEARCH.md
@.planning/phases/03-planning-workflow/03-01-SUMMARY.md

@src/App.tsx
@src/hooks/useTargets.ts
@src/hooks/useEphemeris.ts
@src/components/ui/FocalPlaneView.tsx
@src/components/scene/WFIFootprint.tsx
@src/components/layout/AppLayout.tsx

<interfaces>
<!-- From Plan 01 outputs (created in 03-01) -->

From src/lib/rollRange.ts:
```typescript
export function computeRollRange(
  targetRa: number, targetDec: number,
  sunRa: number, sunDec: number
): { nominal: number; min: number; max: number } | null;
```

From src/lib/urlState.ts:
```typescript
export interface ObservationParams {
  ra?: number;
  dec?: number;
  name?: string;
  pa?: number;
  date?: string;
}
export function encodeToHash(params: ObservationParams): string;
export function decodeFromHash(hash: string): ObservationParams;
```

From src/lib/ds9Export.ts:
```typescript
export function generateDS9Regions(targetRa: number, targetDec: number, v3pa: number): string;
export function downloadDS9Regions(targetRa: number, targetDec: number, v3pa: number, targetName?: string): void;
```

<!-- From existing codebase -->

From src/lib/siaf.ts:
```typescript
export const FPA_ROTATION_DEG = -60.0;
```

From src/lib/coordinates.ts:
```typescript
export function positionAngle(targetRaDeg: number, targetDecDeg: number, sunRaDeg: number, sunDecDeg: number): number;
```

From src/hooks/useTargets.ts:
```typescript
export interface Target { id: string; name: string; ra: number; dec: number; addedAt: number; }
// useTargets returns: { targets, selectedTarget, selectedTargetId, addTarget, removeTarget, selectTarget }
```

From src/hooks/useEphemeris.ts:
```typescript
// useEphemeris returns: { epoch, setEpoch, sunPosition, antiSunPosition }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift PA state to App and create PA slider + share/export UI</name>
  <files>src/components/ui/PASlider.tsx, src/components/ui/ShareButton.tsx, src/components/ui/ExportButton.tsx</files>
  <action>
    Create three new UI components:

    **PASlider.tsx** -- Interactive PA slider showing Sun-constrained roll range:
    - Props: `{ nominalPA: number; minPA: number; maxPA: number; currentPA: number; onPAChange: (pa: number) => void; observable: boolean }`
    - When `observable=false`: show disabled slider with "Target not observable" message
    - Slider operates in offset space internally: value is [-15, +15], displayed PA is `(nominal + offset) % 360` to avoid 0/360 wrap-around issues (per research pitfall #1)
    - Show slider track with markers at min, nominal (center tick), and max
    - Display both V3PA and APA (= V3PA - FPA_ROTATION_DEG which equals V3PA + 60) values. Import FPA_ROTATION_DEG from siaf.ts.
    - Label: "V3PA: {value}" and "APA: {value}" below the slider
    - Add a "Reset" button (or double-click nominal tick) that resets to nominal PA
    - Style: dark theme consistent with existing UI (bg-slate-800/900, text-slate-200/300, cyan accent for slider thumb)
    - Add note below: "Roll range approximate; verify with APT" (per research recommendation)

    **ShareButton.tsx** -- Copy URL to clipboard:
    - Props: `{ url: string }` (the full URL with hash, computed by parent)
    - Uses `navigator.clipboard.writeText(url)` on click
    - Shows brief "Copied!" feedback for 2 seconds (useState + setTimeout)
    - Icon: Link or Copy from lucide-react
    - Style: small icon button, consistent with existing Header button style

    **ExportButton.tsx** -- Download DS9 region file:
    - Props: `{ targetRa: number; targetDec: number; v3pa: number; targetName?: string; disabled?: boolean }`
    - On click: calls `downloadDS9Regions(targetRa, targetDec, v3pa, targetName)` from ds9Export.ts
    - Icon: Download from lucide-react
    - Label: "Export DS9" or just icon with tooltip
    - Disabled when no target is selected
    - Style: consistent with ShareButton
  </action>
  <verify>
    <automated>cd /workspace/RomanView && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Three new components compile with no type errors. PASlider renders slider with range markers. ShareButton copies URL. ExportButton triggers download.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PA state, URL sync, and update WFIFootprint/FocalPlaneView</name>
  <files>src/App.tsx, src/components/ui/FocalPlaneView.tsx, src/components/scene/WFIFootprint.tsx</files>
  <action>
    **App.tsx modifications** -- Central state management and wiring:

    1. **PA state lifting**: Add `paOverride` state (number | null). Compute `nominalV3PA` using `positionAngle(target.ra, target.dec, sun.ra, sun.dec)` when target exists. Compute `rollRange` using `computeRollRange(target.ra, target.dec, sun.ra, sun.dec)`. Derive `effectiveV3PA = paOverride ?? nominalV3PA`.

    2. **URL state sync (avoiding infinite loop per research pitfall #3)**:
       - Parse hash on initial render using lazy `useState` initializer for target state. If hash contains ra/dec, use `addTarget(name, ra, dec)` and set PA override from hash.pa, and epoch from hash.date.
       - Use `useEffect` to update URL hash when target/PA/epoch changes. Use `history.replaceState(null, '', newHash)` (NOT direct hash assignment) to avoid triggering hashchange events and infinite loops.
       - Listen for `popstate` events (back/forward) to re-hydrate state from URL.
       - Guard: Use a `skipNextHashUpdate` ref to prevent effect from firing when URL was the source of the state change.

    3. **Prop threading**:
       - Pass `effectiveV3PA` (not sunPosition) to WFIFootprint and FocalPlaneView as a `v3pa` prop
       - Pass PA slider props: rollRange.nominal, rollRange.min, rollRange.max, effectiveV3PA, setPAOverride
       - Pass share URL: `window.location.origin + window.location.pathname + encodeToHash({ra, dec, name, pa: paOverride, date})`
       - Pass export props: targetRa, targetDec, effectiveV3PA, targetName
       - Add PASlider to sidebar (below EpochSlider)
       - Add ShareButton and ExportButton to header or sidebar toolbar area

    4. **Reset paOverride to null when target or epoch changes** (so nominal PA recomputes for new context).

    **WFIFootprint.tsx modifications**:
    - Change to accept `v3pa: number` as a prop instead of computing it from sunPosition via positionAngle
    - Remove the `positionAngle` import and internal `v3pa` useMemo computation
    - Use the passed `v3pa` prop directly in all v2v3ToSky calls
    - Update the component's props interface to include `v3pa: number` and remove `sunPosition` if it was only used for PA

    **FocalPlaneView.tsx modifications**:
    - Change to accept `v3pa: number` as a prop instead of computing it internally
    - Remove the `positionAngle` import and internal `v3pa` useMemo
    - Remove `sunPosition` from props (it was only used for PA computation)
    - Use the passed `v3pa` prop for skyToFocalPlane, rotateByPA, APA calculation, and compass display
    - Keep all other rendering logic unchanged
  </action>
  <verify>
    <automated>cd /workspace/RomanView && npx tsc --noEmit && npx vitest --run</automated>
  </verify>
  <done>App compiles with no type errors. PA slider controls footprint rotation in both views simultaneously. URL hash updates on state changes. Loading a hash URL hydrates state correctly. Share button copies URL. Export button downloads .reg file. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` -- zero errors
2. Full test suite: `npx vitest --run --reporter=verbose` -- all tests pass
3. Dev server: `npx vite --open` -- app loads without console errors
4. Manual smoke: change target, drag PA slider, verify footprint rotates in sky and focal plane views
</verification>

<success_criteria>
- PA slider renders with correct roll range bounds from computeRollRange
- Dragging PA slider rotates footprint in both sky view and focal plane view in real-time
- PA slider disabled with message when target is not observable
- V3PA and APA values displayed and update as slider moves
- URL hash updates silently when target/PA/epoch changes
- Opening a URL with hash params hydrates the correct target, PA, and date
- Share button copies URL to clipboard with brief confirmation feedback
- Export button downloads a .reg file
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-workflow/03-02-SUMMARY.md`
</output>
