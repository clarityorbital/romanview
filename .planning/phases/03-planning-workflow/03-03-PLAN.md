---
phase: 03-planning-workflow
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03]

must_haves:
  truths:
    - "PA slider shows constrained roll range and footprint rotates when dragged"
    - "Shared URL reproduces exact same view in a new tab"
    - "Downloaded .reg file contains 18 SCA polygons in FK5 coordinates"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 3 features: PA slider interaction, URL sharing, and DS9 export.

Purpose: Confirm that the planning workflow features work correctly end-to-end from a user's perspective before marking Phase 3 complete.

Output: Verified confirmation that PLAN-01, PLAN-02, and PLAN-03 requirements are met.
</objective>

<execution_context>
@/home/vscode/.claude/get-shit-done/workflows/execute-plan.md
@/home/vscode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-planning-workflow/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of Phase 3 planning workflow</name>
  <files></files>
  <action>
    Present the verification checklist to the user and start the dev server.

    What was built:
    - Interactive PA slider with Sun-constrained roll range (+/-15 deg)
    - Shareable URL encoding (target, PA, date in hash)
    - DS9 region file export (.reg with 18 SCA polygons)

    Start dev server: `cd /workspace/RomanView && npx vite`

    **Test 1: PA Slider (PLAN-01)**
    1. Open http://localhost:5173 in browser
    2. Add target "M31" via search
    3. Verify PA slider appears in sidebar with roll range labels (V3PA and APA values shown)
    4. Drag slider left and right -- footprint should rotate visibly in BOTH sky view and focal plane view
    5. Verify slider range is approximately 30 degrees wide (nominal +/-15)
    6. Click "Reset" or double-click center to return to nominal PA
    7. Try a target near the Sun exclusion zone -- slider should show "not observable" message

    **Test 2: URL Sharing (PLAN-02)**
    1. With M31 selected and PA adjusted via slider, copy the URL from address bar
    2. URL should contain hash params like #ra=10.68470&dec=41.26870&name=M31&pa=...
    3. Click "Share" button -- should copy URL to clipboard with brief "Copied!" feedback
    4. Open new browser tab, paste the URL
    5. New tab should load with M31 at the same PA and date as the original tab
    6. Verify RA/Dec, PA slider position, and footprint orientation match the original

    **Test 3: DS9 Export (PLAN-03)**
    1. With a target selected, click "Export DS9" button
    2. Browser should download a .reg file
    3. Open the .reg file in a text editor
    4. Verify: header says "DS9 version 4.1", coordinate system is "fk5"
    5. Verify: 18 polygon lines exist, each with text={WFI01} through text={WFI18}
    6. Verify: each polygon has 4 coordinate pairs (8 numbers)
    7. Verify: RA values are reasonable (near target RA), Dec values near target Dec
    8. (Optional) If DS9 is available: load the .reg file and verify 18 SCA outlines appear at correct sky position

    **Test 4: Edge Cases**
    1. Open app with no hash (fresh URL) -- should work normally with default behavior
    2. Open app with partial hash (#ra=10.68) -- should handle gracefully
    3. Change epoch slider -- PA slider range should update (Sun position changes)
    4. Remove target -- PA slider should disappear or disable, export button disabled
  </action>
  <verify>User confirms all tests pass by typing "approved"</verify>
  <done>All Phase 3 requirements (PLAN-01, PLAN-02, PLAN-03) verified end-to-end through interactive testing. PA slider works, URL sharing works, DS9 export works.</done>
</task>

</tasks>

<verification>
All three Phase 3 requirements verified through interactive testing:
- PLAN-01: PA slider with Sun-constrained roll range and real-time footprint rotation
- PLAN-02: URL state encoding/decoding with share button
- PLAN-03: DS9 region file export with correct FK5 polygon format
</verification>

<success_criteria>
- User confirms PA slider rotates footprint in real-time with correct roll range
- User confirms shared URL reproduces identical view in new tab
- User confirms DS9 export downloads valid .reg file with 18 SCA polygons
- No console errors during testing
- Edge cases handled gracefully (no target, partial URL, epoch changes)
</success_criteria>

<output>
After completion, create `.planning/phases/03-planning-workflow/03-03-SUMMARY.md`
</output>
